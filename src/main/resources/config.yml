# BTEMoreEnhanced config
# -------------------------------------------------- #
# update check
# UpdateCheckEnabled: true (If plugin will check for new GitHub releases and notify)
  UpdateCheckEnabled: true
# -------------------------------------------------- #
# treepacks
# TreepackFolder: newtrees (Default treepack folder name)
# TreepackAddons: addon1,addon2 (Addon(s)' folder(s) name(s))
  TreepackFolder: ""
  TreepackAddons: ""
# -------------------------------------------------- #
# //wood
# MaxSchemSize: 5120 (In bytes)
# MaxTries: 60 (Bridons algorithm tries to find a suitable next point for a tree. The narrower/smaller the selection and larger the radius the less likely a point is found per try.
  MaxSchemSize: 5120
  MaxTries: 60
# -------------------------------------------------- #
# //terraform
# DefaultTopRemove: 15 (what value will //terraform command suggest in tab completion, -1 to disable)
# DefaultBotRemove: 10 (what value will //terraform command suggest in tab completion, -1 to disable)
# DefaultBlock: "emerald_block" (surface block for //terraform, values are just ingame text ids)
# DefaultBiome: "none" (none for not changing biome, values are just ingame text ids)
  DefaultTopRemove: 15
  DefaultBotRemove: 10
  DefaultBlock: "emerald_block"
  DefaultBiome: "none"
# -------------------------------------------------- #
# selection shortcuts
# DefaultShortcutEnabled: true (default toggle for wooden axe air-click shortcuts)
# DefaultShortcutIncrement: 1 (default positive increment for //expand, //shift and //contract shortcuts)
# ShortcutIncrementMax: 10 (maximum allowed value for player's ShortcutIncrement setting)
  DefaultShortcutEnabled: false
  DefaultShortcutIncrement: 1
  ShortcutIncrementMax: 10